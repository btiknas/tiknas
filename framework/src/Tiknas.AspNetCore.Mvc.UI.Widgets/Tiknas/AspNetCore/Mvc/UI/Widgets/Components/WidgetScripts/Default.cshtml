@model Tiknas.AspNetCore.Mvc.UI.Widgets.Components.WidgetResourcesViewModel
@if (Model.Widgets.Any())
{
    var resourceItems = Model.Widgets.SelectMany(w => w.Scripts).ToArray();
    if (resourceItems.Any())
    {
        <tiknas-script-bundle>
            @foreach (var resourceItem in resourceItems)
            {
                if (resourceItem.Src != null)
                {
                    <tiknas-script src="@resourceItem.Src" />
                }
                else
                {
                    <tiknas-script type="@resourceItem.Type" />
                }
            }
        </tiknas-script-bundle>
    }
}